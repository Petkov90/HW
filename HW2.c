
#include <stdio.h>

int main() {
	int side1, side2, lice, obikolka;
    side1 = 3; 
    side2 = 5; 
    lice = side1 * side2;
    obikolka = side1 * 2 + side2 * 2;
        printf(" lice:%d\n obikolka:%d\n ", lice , obikolka);
return 0;
}